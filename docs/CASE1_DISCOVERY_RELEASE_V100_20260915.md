# 完整成果专家评审：v1.0.0 发版修订

日期：2026-09-15。用户要求维持客户端版本 **v1.0.0**，不使用上一轮自行递增的 1.0.1。

## 本次变更

- 删除入口中“这是问卷试用，不是正式研究招募”及其后整段材料选取、方法比较和认证提示；同时删除加载配置后重新填入该提示的代码。
- 中英文同意填写、连接服务、完成记录和错误提示统一使用“评审/问卷”，不再称为“试用”。保留实际数据保存、退出与删除请求说明。
- 删除没有依据判断其当前状态的“伦理、正式招募等尚未完成”表述；没有改写为“已经通过伦理审批”。软件发版不替代研究审批或证据核查。
- 客户端 package、preload、运行时与页面版本均为 **1.0.0**。页面另保留原材料版本，二者不混用。
- 十案例、每例六题、单轮阅读、1–5 评分与唯一不计分选项保持不变。继续支持随时切换中文/English，客户端启动不弹问卷邀请。
- 更新运行时清单指纹，确保旧的同版本缓存不会遮蔽新页面。未安装本构建，未自动退出或重启用户客户端。

## 本次分发目录

`C:/Users/45846/Documents/Code/NeuroClaw/desktop/dist/study-client-v1.0.0-20260915`

- `NeuroDiscovery Setup 1.0.0.exe`：Windows x64 安装版。
- `NeuroDiscovery 1.0.0.exe`：Windows x64 免安装版。
- `NeuroDiscovery-1.0.0-win.zip`：完整便携目录。
- `BUILD_INFO.json`：本次文件哈希与实际检查记录。

使用本目录的新文件，而非 `dist/v1.0.0` 的历史包或 `dist/study-client-1.0.1`。旧文件保留，不覆盖历史交付。

## 使用入口

打开本次客户端后，从顶部 **NeuroDiscovery → 专家研究 / Expert Study** 进入。问卷随客户端打包，不依赖独立的 8769 预览服务。中文/English 按钮在问卷顶部；不会因为切换语言而重载或清空当前评审。

## 不变的数据与记录

以已核验的 Windows 1.0.0 为基础，仅保留上一轮已验证的问卷功能和本次文案修订，不混入脏工作区的其他功能。没有运行或改写神经影像实验，没有采用仍在途的第二波材料。

原作者材料 SHA256 仍为 `c99f1765154ad2eec1e0a82464ff8c2c1d261bfd681c4275eaabf728beb288f4`。分发时继续使用去除私有整理者映射的独立材料包，其题目、科学材料与计分逐值不变。原材料标识、内部 `pilot_only`/`record_kind` 历史编码和旧答案不因界面发版而重标记。它们是兼容性与溯源字段，不是客户端显示版本；旧问卷和新的正式收集记录不能仅凭本次 UI 文案变化就视为同一招募批次。

## 检查与交付边界

- 源码回归：106 项 Python、14 项 Node 测试通过，包括删除通知后的配置加载，不因访问已删除 DOM 节点而失败。
- 实际打包后端：鉴权、10 案例/60 题合成保存提交、双语显示来源记录、导出和无效选项排除通过。测试数据独立存放，未打入客户端。
- 实际 ASAR：1.0.0 版本、菜单→preload→内嵌路由、重复进入不重载、运行时缓存指纹变化通过；菜单链路以隔离 Electron 替身验证。
- ZIP 与安装器/免安装版载荷检查见 `BUILD_INFO.json`。不存在已填写问卷、整理者私有映射或新增实验结果。
- 本次 browser/computer-use 连接重试及重置后仍不可用，未获得新的视觉截图，不宣称做过本轮原生窗口或浏览器 GUI 验证。
- 已生成 Windows 文件，未自动安装，未推送 Git 或发布到 GitHub。文件未数字签名；本次没有生成 macOS 安装包。

本次仅改变发布界面及包装，不将专家评分解释为统计验证、伦理审批或首次发现认证，也没有删除案例内部的真实证据限制。
