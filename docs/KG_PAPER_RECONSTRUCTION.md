**论文知识层重建：当前执行方向**

**2026-09-22 仲裁职责更新。** 用户指定当前主助手root作为矛盾的最终仲裁者。见 `KG_AUTOMATION_RESTART_PLAN_20260922.md` 第5节：依据原文核对并记录可追溯裁决，允许保留真实冲突和证据不足，不以模型投票替代判断。普通科学争议由root处理；预算、权限和发布边界不变。当前未接通后台自动root仲裁，不能据此声称无人值守流程已完成。

**2026-09-22 零继承自动化重建计划。** 用户要求“继续计划一下，将我们现有的一点点进度丢弃”。最新方案为 `KG_AUTOMATION_RESTART_PLAN_20260922.md`，覆盖下方继续手工及复用28篇成果的安排：旧28篇/159观察/72命题/120链接/8多作品命题仅保留历史档案，不进入新运行基线、答案或计数；保留源库、固定层边界、40篇heldout和费用审计。新运行将从原文重抽取，以跨论文全局合并为核心，执行Ollama优先、额度耗尽切NVIDIA、短周期恢复后有界回切且尊重周/月限制的通用规则。本次仅计划，未清空数据库、创建运行或开启外部派发。旧STATE/config仍记录旧手工运行，不代表新方案已经启动。

**2026-09-22 自动化设计更新。** 用户要求设计高效、可自动运行的完整流程，而非继续逐次手工写几篇论文。端到端方案见 `KG_AUTOMATION_PLAN_20260922.md`：核心是跨论文语义合并、全局注册表、可恢复调度与误并/漏并验收。本次仅设计，未启动任务或恢复外部调用；当前禁用配置不变。无需凭证的工程可先建设，自动语义执行环境仍须明确，不能将当前会话手工阅读描述为已经可后台调用的服务。下方“root手工”是当前配置状态，不是新方案的长期吞吐设计。

**2026-09-22 执行方式更新。** 用户要求“我们不需要 provider key，你自己手工做”。当前改为 root 直接阅读源材料、手工抽取与范围判断；不调用外部模型、Ollama 或 agents，不创建任务/自动化，不涉及 E 盘迁移。provider key 不再是当前工作的前置条件。后文 2026-09-20 的模型执行顺序与费用门仅保留为历史方案，不再要求先运行模型或禁止逐篇手工抽取。旧请求、未知费用预留、编译输入和40篇 held-out 分配保持不变；held-out 暂不用于开发，也不能将同一 root 的抽取与自检报告成独立盲测。继续复用已读25篇和两个冻结批次，从未处理的非 held-out 论文追加独立 staging 批次；科学验收、身份/版本去重、反面证据与固定层保护仍必需。权威授权见 `tmp/kg_paper_reconstruction_20260920/ROOT_MANUAL_AUTHORIZATION_20260922.json`，实际累计数量以 `STATE.json` 为准。全库完成时间不作未经实测的承诺。

2026-09-20，依据用户最新指令。重建对象是论文抽取及其生成的知识层。固定神经科学数据集、已有知识资源、图谱词表、数据集变量、图谱/模态/模型定义和已有静态关系保持原样。

旧补论文、修三元组和规范化归组路线退出当前工作。取消 v29 作为当前任务的版本名称、父版本、重建基线及进度口径，也不延续 v30 等编号。旧文件只作为历史档案保存。新工作从论文源材料出发；已经保存的缓存与书目资料继续复用。历史服务尚未替换，不能把当前入口指向尚未重抽取完成的图。

**完整交付链**

| 阶段 | 工作与输出 | 结束条件 |
|---|---|---|
| 1. 固定数据边界与论文清单 | 保护现有固定层；从已有来源库建立按论文身份组织的队列，保留所有源快照和材料缺口 | 每个已知发布记录有去向；来源身份/版本未知单列；不按 90 万旧 claim 分派工作 |
| 2. 抽取设计及验证 | 执行统一抽取合同：原始观察、共享命题、来源证据、研究条件分层；验证已知重复研究、相反结果、不同指标及多篇论文共同命题 | 能从原文直接得到可共享命题，并保留具体结果；不借用旧 claim 作为答案 |
| 3. 按论文批量重抽取 | 每篇论文的源材料集中处理一次；结果按固定批次写入检查点；同时保留提到的其他论文、版本及队列重叠信息 | 全部队列进入已抽取/材料不足/身份未决/科学未决等明确状态；不把缓存整理计作抽取完成 |
| 4. 全局共享命题与新图构建 | 跨批次复用命题注册表；检索候选后判断语义关系；按真实作品去重证据；以独立论文层关联既有固定层 | 相同命题能直接展开多篇论文，各自的条件、方向、原句、数值和证据角色均可见 |
| 5. 验收、接入与交付 | 校验固定层逐记录无变化；验证论文去重、错并/漏并、原文锚点、查询与导出；整体替换论文知识视图 | 完成数据和科学验收；发布一次；保存原材料、构建标识、未决清单和可回退档案 |

实现顺序按上表持续推进，不为每个小批重新制定计划或发布一次。不复用旧工作“原观察完整升级 10,000 条”的计数口径来评价新抽取，也不因为旧规则的修订次数用尽而阻止这项新设计。旧报告与门槛保留历史原文，不能改写成已通过。

新目标仍然是：论文抽取不再把同一个科学命题拆成大量单论文 claim。验收同时报告全量论文处置覆盖率、每个共享命题的不同论文数、多论文命题比例、支持/相反/部分/背景/未决分布、语义误并与漏并及元数据保留。真实单篇命题允许保持单篇；不能为了比例将无关论文挂进同一命题。正式质量样本与阈值在新抽取规则冻结前确定，不能观察结果后改变。

**固定层的保护边界**

固定层包括 NeuroNames、MeSH/UMLS 词表、DisGeNET、CognitiveAtlas、BrainMap、ATC、AHBA、受体分布、现有数据集变量与基础设施等实际已导入内容；是否固定依据现有来源与构建记录，不能只看节点名或节点是否带 DOI。部分固定数据本身来自发表过的论文，这不使其成为本次重抽取对象。

保留节点 ID、名称、别名、映射、metadata，及关系类型、权重、来源和方向。新论文层可以引用这些 ID，但不能覆盖或重算它们。固定记录和论文抽取记录在旧图中混合的，按来源建立边界清单；归属不清的保留并标记，禁止直接删除。

2026-09-20 已完成一次原图顺序扫描、完整哈希验证和来源归属清点，见 `tmp/kg_paper_reconstruction_20260920/FIXED_BOUNDARY.json`。固定参考节点45,105/边55,444，另保护13个共享规范实体、156,186个既有映射节点/249,032条映射边。原图及内容均未修改。后续读取已建索引，不重复扫描原图；整体接入时仍须核验保护记录及边的数量、重复边和端点。

**抽取与合并的核心改变**

每篇论文首先产出具体观察和可复用科学命题。共享 claim 描述“什么变量/干预与什么指标/结果存在什么关系”；研究中的样本量、效应量、p 值、年龄分布和实验参数放在证据层。病种、物种、组织、测量和必要比较对象通过明确的命题限定或证据适用范围保留。

“论文观察较具体”和“共享命题可较一般”可以并存，但一般化必须有明确的逻辑依据与范围，不等于删掉词语。每项证据分别标记完整支持、反对、部分支持、背景或未决。一个复合句可以拆成多个原子观察；各观察仍绑定同一原句和同一论文，不增加作品数。

抽取时检索已有命题注册表，明确选择复用、创建、相关或未决；跨批次再进行集中语义检查。相似度、共享实体 ID、同一句原文都只能帮助召回候选。固定层实体被映射到同一 ID，也不能抹去不同脑区指标、体液、测量方法或因果强度的差异。

原句和完整原始抽取保留；无法规范化的字段进入 extras 与缺失状态。独立队列数未知时保持未知。综述和本人实验分别展示，预印本/正式版不得重复增加作品数。旧 claim 与旧审核只保留历史溯源，不作为重抽取标准答案或新证据信用。

**速度与执行约束**

已经保存的来源库只读复用，按论文而非按旧 claim 读取和抽取；相同源快照只建立引用。分批提交结果、断点恢复、全局注册表和一次正式切换减少重复工作。时间与 token 成本必须根据真正完成的论文抽取吞吐估计；建队列的速度不能当作科学抽取速度。

用户现已明确授权“批量模型抽取，我负责规则、抽查与验收”：由模型批量抽取，root 制定规则、抽查并验收。继续不调用 Ollama、其他 agent，不创建任务或自动化。旧抽取器不作为本流程入口。已完成25篇源摘要的人工开发验证，形成140条观察、54个命题、102条证据连接；8个命题已有2–5篇论文的原始研究支持。首批按主题选择，不能据此外推全库命题比例或准确率。见 `SOURCE_EXTRACTION_REPORT.md` 和 `DEVELOPMENT_CHECKPOINT.json`。

**当前执行状态。** 394,952个工作项中，321,874篇可用摘要已经一次性编译为独立、可复用的批量输入；25篇开发抽取不重复派发；64,282项缺缓存、7,921项身份未决、850项版本未决各自保留。原始书目、源快照和全部 metadata 继续保存在只读来源库中。批量输入只含源文本、文章类型及更正/撤稿信息，不含旧 claim 答案，也不重复读取整个图。

批量执行器记录不可变请求、原始输出、源 SHA、实际返回模型、费用估计和断点；未决请求不自动重发，401/403 自动停止对应通道。初期技术实测暂限 USD5 保守费用估计和60次请求；这不是全库支出的许可。首条通道12次调用未得到模型输出；另一处既有服务明确返回迁移地址，迁移后的1次实际调用仍返回无效令牌401。共13次尝试、0次成功抽取，两个通道均已停止，等待有效凭证。未知费用保留预留额，不宣称实际扣费为零。

**从当前检查点直达交付的执行顺序：**

1. 有效密钥到位后先完成一次实际抽取，再处理固定的6篇开发病例。选择模型依据原文忠实度、结果遗漏和实际成本，不依据能否访问模型列表。每次更换服务/模型/提示均保存新绑定，失败历史不覆盖。
2. 完成事先固定的40篇未见论文抽取审核，并在模型归组判断前冻结40组语义比较。程序检查原句与数值；root 审核身份、结果方向、证据角色、适用范围及遗漏。严重错误为0、观察忠实度至少98%、主要结果召回至少90%；语义误并为0。开发样本不能充当独立盲测成绩；小样本通过不等于证明全库准确率。
3. 用实测 token、时延、服务限额及返工率给出全库成本和工期，确定总支出上限后按现成输入批量运行。root 按冻结方案分层抽查；不回到逐篇人工编写全部论文。失败、材料不足和科学未决单列；只重试明确允许的失败，不重复成功批次。模型来源和审核覆盖率如实标记，不伪装成逐篇人工阅读。
4. 全局共享命题注册表统一处理复用、创建、相关与未决。按规范实体、测量及范围检索候选，再做语义判断，不能仅凭相似度合并。保留每篇论文各自的观察、结果、反面证据、范围和 metadata；同作品版本不增加论文数，队列独立性未知仍保持未知。
5. 对新论文层做数据与科学验收，逐记录验证固定层及既有映射完整，完成查询/展开证据/导出及影子验证后整体接入。交付真实多论文 claim 增量、单论文命题、未决项及元数据覆盖报告，并保留可回退档案。当前尚未进入正式接入，也没有新增生产 claim。

批量工件位于 `tmp/kg_paper_reconstruction_20260920/BATCH_MODEL/`：授权 `AUTHORIZATION.json`、盲测协议 `EVALUATION_PROTOCOL.json`、输入 `INPUTS.sqlite`、覆盖及完整性凭据 `INPUTS_READY.json` / `INPUTS_INTEGRITY.json`、请求账本 `REQUESTS.sqlite`。正常续跑只读取这些小凭据和所需输入，不重建已完成队列。

当前入口：[任务配置](C:/Users/45846/Documents/Code/NeuroClaw/neurooracle/configs/kg_reconstruction.json)、[抽取合同](C:/Users/45846/Documents/Code/NeuroClaw/docs/KG_PAPER_EXTRACTION_CONTRACT.md)、[运行状态](C:/Users/45846/Documents/Code/NeuroClaw/tmp/kg_paper_reconstruction_20260920/STATE.json)。
