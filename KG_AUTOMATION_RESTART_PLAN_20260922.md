# 论文知识层：零继承自动化重建计划

日期：2026-09-22。依据用户“继续计划一下，将我们现有的一点点进度丢弃”。

状态：计划已更新，运行尚未启动。取代 `KG_AUTOMATION_PLAN_20260922.md` 中复用手工抽取成果、排除已读论文及引擎未选定的安排；其他科学保护原则继续有效。本次不删除数据、不清空数据库、不启用模型执行、不创建后台任务。

## 1. 丢弃什么，保留什么

**新运行从零计数，不继承现有科学成果。** 原28篇、159条观察、72个命题、120条证据链接、8个多作品支持命题全部退出新基线。不导入新注册表，不充当参考答案，不授予新运行完成或验收信用。

“丢弃进度”在此落实为逻辑退役而非物理删除：旧 `LITERATURE.sqlite`、批次01–03、导入回执和手工检查点原地留作只读历史。新结果进入独立运行空间，不覆盖、重生成或续写旧批次，不把旧状态文件中的28改成0来篡改历史。

继续复用的是基础设施，不是科学结论：

- 原始论文缓存、CORPUS、书目索引、来源快照与身份记录，不重复抓取已有材料或重扫原图。
- 固定神经科学层、保护分区、边界回执和实体参考目录，保持只读，不重新摄取。
- 冻结的321,874条源输入；缺少编译输入的适格论文只生成本运行的增量源包，不重建整个 INPUTS。
- 40篇 heldout 的原有分配及未消费状态；旧13条 HELD 请求、未知费用预留和失败历史原样保存。
- 可复用的验证与恢复代码，经新接口测试后采用；旧39项技术测试不计作新流水线或科学验收通过。

原28篇可以在新流程中从原文重新抽取，但原输出不能进入提示、合并候选或评分参考。它们已被人工阅读，属于已暴露的开发材料，不可重新包装成盲测样本。

新账本的抽取、观察、命题、证据链接、验收和发布计数均从0开始。历史与新运行分栏展示，绝不相加。

## 2. 交付目标

交付的是一个能持续汇聚跨论文发现的论文知识层，不是批量制造互不关联的三元组。

结构为：`来源/作品/版本 → 原子观察 → 共享命题 → 分方向证据 → 研究主题与命题关系`。

- 同义且必要范围相同的发现复用一个稳定命题；观察保留具体样本、数值、时间点、原句和方法。
- 支持、反对、未显著、部分、背景和假说分别保存。无显著差异不等于相等；综述不算独立原始实验。
- 范围更窄、更宽、机制相关但不等价的发现建立关系，不强行合并；不以最大多论文比例为目标。
- 不混淆物种、亚组、指标、脑区、剂量、因果与相关。版本去重、作品去重和队列独立性分开处理。

## 3. API 执行策略

遵守 `C:\Users\45846\Documents\Code\cc_memory\ROG\fb_api_routing.md`：

1. 优先 Ollama `deepseek-v4.1-flash:cloud`，`think: high`；依顺序使用已授权且有可用额度的凭据。
2. 无可用额度时切 NVIDIA 网关的精确路由 `openai/openai/gpt-5.6-sol`，`reasoning_effort: high`。
3. 五小时额度恢复后有界尝试回切；周/月额度仍耗尽的凭据不能仅因短窗口恢复而重试。恢复时间未知则明确未知，不编造重置时刻。
4. 不自动换 Azure 或其他模型；不记录密钥正文。按请求记录提供商、模型、参数、用量、费用状态及切换原因。

两条路由均需分别通过抽取与合并门槛。连通性成功不是科学合格，提交 high 参数也不证明内部推理强度。备用路由未通过质量门时暂停该类任务，而不是为保持吞吐接受未验证输出。验证还须覆盖跨模型、跨批次共用注册表的情况。

预算按运行统一累计，切模型/凭据不重置计数。新建传输绑定与运行授权，不恢复旧失败请求或挪用旧 USD5/60请求许可。本计划不自动打开现有禁用开关。

## 4. 六个阶段，一套连续流程

### 阶段一：独立运行空间与可恢复账本

- 实施时创建独立候选库、规范命题库、请求/费用账本、任务队列及运行清单。清单固定来源绑定、合同、代码、模型策略和审计协议版本。
- 从现有源身份和可用性派生本运行任务，不能用旧 `abstract_extracted_staging` 状态跳过28篇。也不能只复制321,871个旧待读项当作新的全部范围。
- 先对账：旧28篇中哪些已有编译输入，哪些需要增量源包；heldout、身份待核实、版本待核实、缺来源分别隔离。准确可调度数量在这一步测得，不提前宣称全量已就绪。
- 请求与任务分别记账。原子领取、租约、短事务、唯一键及单注册表写入器保证本地有效结果不重复提交；远端执行未知进入 HOLD，不因租约到期或换路由盲目重发。

交付门：本地故障恢复、重复提交、并发冲突、配额切换、未知费用隔离和只读边界测试通过。

### 阶段二：源驱动抽取与结构验证

- 默认一篇源包一次抽取，保存不可变原响应和规范化候选。长材料分段须保留原始坐标及全篇覆盖账本，不静默截断。
- 模型不接触旧 claim 答案或旧28篇输出。先抽取观察，再匹配新运行的命题注册表，避免按已有目标迎合抽取。
- 校验来源哈希、引用锚点、数值、角色、必要条件和缺失字段；语法或原句存在只能证明结构约束，不能代替语义核验。
- 自动候选、自动门通过、人工抽查通过与独立验证严格区分，不伪造 root-reviewed 标签。

交付门：两路模型在冻结源材料和评分协议下分别达到抽取门槛，失败结果进入隔离区而非静默丢弃。

### 阶段三：全局共享命题，先证明合并能力

- 范围化倒排检索与同义实体映射负责候选召回，可选向量通道须另行绑定允许的引擎与成本；不做全库平方级配对。
- 初始 top-k 是性能参数，不是科学保证。未知条件不能硬过滤，反向和未显著结果也必须召回；用冻结正例测候选召回。
- 对候选输出 equivalent、narrower、broader、related、incompatible 或 uncertain，另行记录证据方向，保留原文依据与条件比较。
- 每条新观察对照稳定命题定义，禁止用近似关系的传递闭包自动合并。串行注册表处理并发新建，merge/split/alias 可追溯、可撤销且无循环。
- 持续做跨批次归并，阶段末做全局漏并审计；同作品多个版本不增加支持数，未知队列独立性不算独立重复验证。

交付门：冻结正负例、条件冲突和反证集上同时测误并、漏并与整簇范围一致性。不能只展示几个成功簇。

### 阶段四：一次有界端到端验证

建议一次规划好的1,000篇非heldout源论文验证批，而非每几篇重新规划。内部小块提交只用于恢复，不逐小批发布。

- 使用新的源驱动参考集，不继承旧28篇标签。主题富集样本验证共享命题，分层随机样本检查一般抽取表现；二者分别报告，主题样本不能估计全库多论文率。
- 先冻结样本、标签、合并正负例、错误分类与阈值，再预测。分别评估两条路由，并检查输入顺序和提供商切换导致的命题分裂或误并。
- 调参只用开发部分；最终采用原40篇heldout及预先冻结的40组语义比较进行验收，进入该阶段前落实验证授权和参考判断。当前不读取heldout正文、不消费分配。
- 沿用既定门：接纳的锚点/数值绑定100%，样本内严重身份/角色/方向/范围错误0，观察忠实度至少98%，显著结果召回至少90%，错误等价合并0。
- 新增提案：真正等价正例最终归入同一命题的召回至少90%，同时报告候选召回和判定召回。须在预测前冻结，不是既有成绩。样本量、置信区间及参考来源须随结果报告；小样本不能证明全库98%准确。
- 测量抽取、合并、写入、人工审核及暂停在内的端到端有效吞吐和成本。1,000篇是提议范围，实际调用还受请求、token、费用与时间硬上限约束。

交付门：质量通过，预算可承受，审核积压可控，才能进入全量。失败即修复和重新冻结受影响版本，不降低阈值掩盖失败，不拿已暴露heldout反复调参冒充盲测。

### 阶段五：持续处理全部适格来源

- 同一协调器在授权上限内连续推进，采用动态并发、token限速、待合并/待审核背压和磁盘下限，不靠扩大请求量掩盖下游瓶颈。
- 分开记录每个模型及混合路由的质量与产量，固定分层抽样频率。严重错误暂停受影响版本及其下游，不继续扩散。
- 无缓存来源、身份/版本未决保留独立清单。补源需要另一个明确有界的来源获取阶段，不把这些项目记作已抽取。
- 完成量按本运行精确状态对账：抽取成功、未开始、处理中、缺来源、身份/版本HOLD、执行未知和失败各有去向；另计合并、审核、验收、发布进度。

交付门：所有范围有明确处置，实际覆盖及残余缺口透明，全局误并/漏并审计通过。空队列不等于全库科学完成。

### 阶段六：影子集成与一次验收发布

从新接受层构建影子论文知识层；检查固定层无变化、查询/原句展开、别名、方向、数值、作品计数及导出。验收通过且获得相应发布许可后原子切换，保留回滚。旧手工暂存结果不在此步骤重新混入。

## 5. 矛盾处理：root 最终仲裁

用户于2026-09-22明确要求：“如果遇到矛盾，我希望你作为最权威的仲裁”。当前主助手（root）承担本项目流程内的最终科学仲裁，不把普通抽取/合并争议逐项退回用户，也不按模型多数票或提供商等级决定真伪。这是流程决策权限，不代表助手具有高于原始证据的科学权威或不会出错。

### 提交与裁决

- 抽取、复核或合并判断冲突时，生成源绑定争议包：双方判断、来源哈希与原文定位、必要上下文、数值和条件、作品/版本身份、关联命题及影响范围。复核时尽可能先读原文再看模型结论，避免被答案锚定。
- root 直接检查证据，区分抽取错误、身份/版本错误、范围不同造成的表面冲突，以及范围相同但研究结果确实相反。按问题需要核对方法、样本、调整因素、统计不确定性及更正/撤稿记录；摘要无法回答的事项不得假装完成全文核实。
- 可裁决为纠正抽取、合并等价命题、拆分范围、保留正反证据、请求有界补源或证据不足继续HOLD。真实科学矛盾本身可以是最终结论，不强迫选边；未显著结果不自动变为反证或等价证据。
- 记录裁决者、实际阅读范围、来源版本、理由、未决限制、受影响ID和可撤销变更事件。新证据允许重开仲裁，旧判断与反证不删除。

### 执行边界

- 有裁决回执后，相关记录才能按裁决继续流转。无关任务可继续；若发现系统性错误，暂停受影响模型/合同版本及其下游，不只是隔离单例。
- 当前会话root尚不是后台调度器可自动唤起的服务。未实现并授权相应接入前，争议进入持久化仲裁队列，由root在可用会话中处理；不能声称已无人值守闭环，也不能把一次NVIDIA请求冒充root裁决。
- 仲裁队列设积压上限和背压，按风险优先处理并汇总同类问题；规则级修复仍需逐条重验受影响输出，不用一次裁决给整批自动盖章。
- root仲裁不替代独立科学验证，不修改冻结参考答案来迁就输出，不降低验收阈值，也不解除heldout保护。裁决权不包含增加费用、扩大数据权限或绕过发布许可；这些边界仍由用户确定。

## 6. 开始前需要冻结的运行限制

实现本地接口不等于开始付费科学调用。第一轮执行清单须明确：验证批费用硬上限、最大请求数/总token、每请求输出上限、并发、最长运行时间、未知费用预留、停止条件，以及是否允许随后直接进入全量。

全量费用上限必须单独批准；无法核实实际单价或累计费用时，不报告零成本，也不以请求数替代可靠的货币控制。首次验证测得每千篇包含合并和审核的实际成本后，再给出全量区间估计，不承诺未经测量的完工时间。

## 7. 当前动作与下一交付

此次仅更新计划和入口说明，既有数据库、配置、固定层、生产层及E盘迁移均未改变。旧 `STATE.json` 仍是历史手工运行的真实记录，不是新计划的活动进度。

下一步是实现阶段一至三的可恢复流水线、源绑定适配器及全局注册表，并准备一次完整的有界验证，而不是继续手工第4批。新运行尚未创建，科学成果继承数为0；只有实际运行回执才能开始累加新进度。
